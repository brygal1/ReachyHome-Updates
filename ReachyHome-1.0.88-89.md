# Reachy Home 1.0.88

Hard-cut maintainability Phase 0: unique ADRs and honest gates

* Hard-cut maintainability Phase 0: unique ADRs and honest gates

Give every ADR a unique number, make the architecture ledger match the tree,
delete leftover surfaces, and point local quality at the same classifier CI uses.

* Sync uv.lock metadata written by the current uv runtime

* Close Phase 0 review findings before merge

Fail closed when make quality has no classified paths, keep one XcodeGen pin
outside the grandfathered CLI, drop Swift Dependabot that cannot own project.yml,
and cite Sparkle as ADR-0061.

* Record the XcodeGen 2.46.0 pin in dependency research

* Pin XcodeGen from the commit being released

* Point the Codex Run action at Reachy Home

* Fail-close make quality and hard-cut leftover Phase 0 drift.

The adversarial pass found a real empty-success hole: on macOS Make 3.81 a
failed classifier left targets empty and recurse into install-quality-tools.
Refuse that, cover the worktree Git union, and keep the LOC ratchet on the
repository lane so Apple-only quality still sees it.

Also specialize the two Apple Makefiles, replace the running Home instance on
Run, and stop living docs from advertising a deleted web UI or the progress
diary as the current board.

* Run the ADR uniqueness test from repository quality.

A decisions-only change is quality=true and python=false, so the Python job
never executed the index gate that exists to catch number collisions. The
repository lane already runs on those files.

* Record the official XcodeGen 2.46.0 tag URL.

The pin already named the version and tag object. The dependency ledger also
requires a reproducible upstream URL.

---------

Co-authored-by: Reachy Tests <reachy-tests@example.invalid>
