# Reachy Home 1.0.89

Household readiness owner: one chrome banner, truthful sidebar, one HA fact (#252)

One household readiness state machine owns Reachy Home's chrome: sidebar can no longer contradict the banner stack, at most one software/network banner besides e-stop, Overview 'ready' requires verified identity, and Home Assistant presence reports one connection fact (presence_link) instead of two booleans. Simulation runtimes accept unstamped builds once tunnel, services, and identity are verified; physical robots keep strict commit-sync proof with a truthful unrepairable state. The backend-update deployment sheet is only ever offered when the deployed backend provably accepts this app's pinned target.
